{
  "name": "llm-code-harvester",
  "version": "1.0.0",
  "description": "CLI tool to harvest code snippets from a directory, optimized for LLM export",
  "bin": {
    "llm-code-harvester": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "start": "node dist/index.js",
    "test": "jest"
  },
  "engines": {
    "node": ">=16"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.15.21",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.4",
    "typescript": "^5.8.3"
  }
}
